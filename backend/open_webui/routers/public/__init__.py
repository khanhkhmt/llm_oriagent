# OriAgent Public API v1
# This package provides a stable, secure public API layer for third-party integrations.
# It does NOT replace existing internal APIs (/api/v1/*, /api/chat/*, /ollama, /openai).
